services:
  adexplorer:
    image: simple-adexplorer:latest
    ports:
      - "5000:5000"
    volumes:
      - adexplorer-instance:/app/instance
      - adexplorer-uploads:/app/uploads
    restart: unless-stopped

volumes:
  adexplorer-instance:
  adexplorer-uploads:
